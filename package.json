{
  "name": "reasoning-diff-lab",
  "version": "2.0.1",
  "private": true,
  "type": "module",
  "description": "A local-first research instrument for pilot-testing whether comparing two independently produced structured reasoning paths reveals useful, non-obvious divergences.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.js",
    "start": "node scripts/serve.js",
    "compare": "node cli/compare.js",
    "analyze": "node cli/analyze.js",
    "verify": "npm test && npm run build && node scripts/smoke.js"
  },
  "engines": { "node": ">=20" }
}
